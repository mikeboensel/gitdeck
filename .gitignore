# Dependencies
node_modules/

# Build output
dist/
out/
release/
*.tsbuildinfo
# NOTE: `build/` is electron-builder's buildResources dir (entitlements, icons) —
# it is SOURCE and must stay tracked, so it is intentionally NOT ignored here.

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Local environment / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
*.swp

# Tooling artifacts
.codex
.cache/
coverage/

# Claude Code transient worktrees
.claude/worktrees/**

# Playwright MCP
.playwright-mcp/**
